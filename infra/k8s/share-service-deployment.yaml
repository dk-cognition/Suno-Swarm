apiVersion: apps/v1
kind: Deployment
metadata:
  name: share-service
  namespace: swarm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: share-service
  template:
    metadata:
      labels:
        app: share-service
    spec:
      containers:
        - name: share-service
          image: ghcr.io/suno-swarm/share-service:latest
          ports:
            - containerPort: 4000
          env:
            - name: SWARM_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: share-service-db
                  key: SWARM_DATABASE_URL
            - name: SWARM_API_BASE_URL
              value: https://api.suno-swarm.example
          readinessProbe:
            httpGet:
              path: /healthz
              port: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: share-service
  namespace: swarm
spec:
  selector:
    app: share-service
  ports:
    - port: 80
      targetPort: 4000
